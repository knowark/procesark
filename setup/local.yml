---
- name: Deploy Procesark Server
  hosts: all
  tasks:
    - name: Create Procesark System User
      user:
        name: Procesark
        home: /opt/procesark
        system: yes
        shell: /bin/bash
